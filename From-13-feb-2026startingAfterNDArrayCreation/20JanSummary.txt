1. copy ka logic
2. multiplication legacy 
3. multiplication cache miss.


******************************************************************************
1. copy method reciews following:
        target array and target_from insides vector
        source  array and source_from & source_to insides vector

    NDArray::copy(ndArray4D,{2,0,2,3},ndArray,{1,1},{4,2});
    
    
    
    NDArray::copy(ndArray,{0,0},ndArray4D,{1,0,0,0},{1,0,3,3});
target [0,0]
sourctimindices= source_from=[1,0,0,0] = sourcesize  vector size that held dimensions.
source_to=[1,0,3,3]
last_index= sourcesize-1= 3

while (source_tmp_indeces[last_index] <= source_to[last_index]) {
                    // do whatever you want with current combination stored in tmp_indices
                    value = source.collection[source.getIndex(source_tmp_indeces)];
                    target.collection[target.getIndex(target_tmp_indices)] = value;
                    target_tmp_indices[last_index + offset]++; // Move to next combination in target
                    source_tmp_indeces[last_index]++;  // Move to next combination in source
                }

our requirement is to generate a sequece
say source se pick kr rahe he and soust start he 1000
                                        end he    1033
1st iteration:

                        source me from k  last_index ko ++ kr k source to k last index tk le jana he.
                                to sequece bnana he  and bnega:
                                        1000 1001 1002 1003 and then stop 
                        target start = 0,0 
                                and it generates 00 01 02 03 for its last index.

next                  
  k = last_index - 1; // start from the second last index
                while (k >= 0 && source_tmp_indeces[k] == source_to[k]) k--; // find the rightmost index which can be incremented
                if (k < 0) break; // all combinations are processed
                target_tmp_indices[k + offset]++; // increment this index in target
                source_tmp_indeces[k]++;
                k++;
                while (k <= last_index) {
                    target_tmp_indices[k + offset] = target_from[k + offset]; // reset all indices to the right in target
                    source_tmp_indeces[k] = source_from[k];
                    k++;
                }
k= last_index-1 = 3-1=2 ho jayega 

[1000] 
[10003]

ab jb tk souce  from ke  k index ki value and source ke sourceto index ki value same he k--

value same mtlb combo nahi bn skta


abhi 3rf index k liye 


 1000 1001 1002 1003 


1000
1033

100 ko constant rkh k last wale 0 ko 3 tk
10 ko constant rkh k  2nd last wale ko 0 se 3 
0 1 2 3 
phir isme 0 k liye last wale 0 to 3 jo kr chuke
then 1 kliye last wala 0 to 3
then 2 k liye 
then 3  k liye last walae 0 to 3

phir 




************************************************************************************************************8
ek ibm ka jd dekhte he:
    Required technical and professional expertise
Backend: Java 8+, Spring Framework, Spring Boot, Hibernate/JPA - 
Frontend: JavaScript/TypeScript, React. 
Nice to have skills : HTML5, CSS3 /  
Database: MySQL, PostgreSQL, MongoDB / 
Tools: Git, Maven/Gradle, Docker, Jenkins/GitLab CI /
Cloud: AWS/Azure/GCP experience preferred

